<h1>Rooms</h1>

<ul>
  <% @rooms.each do |room| %>
    <li>
      <%= link_to room.title, room %>
      <p><%= room.description %></p>
      <p><strong>Price:</strong> <%= room.price %></p>
    </li>
  <% end %>
</ul>

<p><%= link_to "Add a New Room", new_room_path %></p>
